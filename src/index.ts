import { PrismaClient } from "../generated/prisma";


const prisma = new PrismaClient();

const main = async () => {
    // const result = await prisma.post.create({
    //     data: {
    //         title: "this is a title 2",
    //         content: "this is a contact ...... 2",
    //         authorName: "Debika Rani"
    //     }
    // })
    // console.log(result)
}

main()